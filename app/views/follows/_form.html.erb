<!-- <% if current_user.follows?(user) %>
  <%= button_to "Unfollow", user_follow_url(user), method: :delete %>
<% else %>
  <%= button_to "Follow", user_follow_url(user), method: :post %>
<% end %> -->

<input type="submit" class="follow-toggle" value="" data-user-id="<%= current_user.id %>" data-initial-followed-state="<%= current_user.follows?(user) ? "followed" : "unfollowed" %>">
